server:
  port: 80

spring:
  application:
    name: shop

  datasource:
    url: jdbc:log4jdbc:oracle:thin:@localhost:1521/xepdb1
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    username: sesac_highend
    password: h12345d

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true
        use_sql_comments: true
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always

  thymeleaf:
    cache: false

  redis:
    host: localhost
    port: 6379

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace

# Spring Security 기본 설정 (Optional)
# spring:
#   security:
#     user:
#       name: user
#       password: password
